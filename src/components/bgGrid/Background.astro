<div class="pixel-background"></div>

<style>
.pixel-background {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  height: -webkit-fill-available; /* Safari fix for mobile */
  min-height: 100vh;
  z-index: -1;
  pointer-events: none;

  /* Base background color that covers everything */
  background-color: #0a0818;
  
  /* Fallback solid color for Safari */
  background: #0a0818;
  
  background-image: 
    /* Large stars */
    radial-gradient(circle at 15% 25%, #8a2be2 1px, transparent 1px),
    radial-gradient(circle at 85% 45%, #9400d3 1px, transparent 1px),
    radial-gradient(circle at 45% 75%, #4b0082 1px, transparent 1px),
    radial-gradient(circle at 75% 15%, #8a2be2 1px, transparent 1px),
    /* Medium stars */
    radial-gradient(circle at 25% 60%, #9370db 1px, transparent 1px),
    radial-gradient(circle at 60% 35%, #da70d6 1px, transparent 1px),
    radial-gradient(circle at 35% 85%, #8a2be2 1px, transparent 1px);
  
  background-size: 
    300px 300px,
    250px 250px,
    280px 280px,
    320px 320px,
    150px 150px,
    180px 180px,
    200px 200px;
  
  /* Ensure background covers entire area in Safari */
  background-repeat: no-repeat;
  background-attachment: fixed;
  -webkit-background-size: cover;
  background-size: cover;
}

.pixel-background::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  height: -webkit-fill-available; /* Safari mobile fix */
  
  background-image: 
    linear-gradient(90deg, transparent 6px, rgba(138, 43, 226, 0.8) 6px, transparent 7px),
    linear-gradient(transparent 6px, rgba(138, 43, 226, 0.8) 6px, transparent 7px);
  background-size: 30px 30px; 
  
  mask: radial-gradient(ellipse at center 60%, transparent 45%, black 75%);
  -webkit-mask: radial-gradient(ellipse at center 60%, transparent 45%, black 75%);
  
  image-rendering: pixelated;
  -webkit-image-rendering: pixelated;
}

/* Additional Safari-specific fixes */
@supports (-webkit-touch-callout: none) {
  .pixel-background {
    height: -webkit-fill-available;
    min-height: -webkit-fill-available;
  }
}

/* Ensure body and html also have the background color */
html, body {
  background-color: #0a0818;
  margin: 0;
  padding: 0;
}
</style>